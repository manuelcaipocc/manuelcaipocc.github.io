---
# To publish author profile pages, remove all the `_build` and `cascade` settings below.
_build:
  render: always #never
sitemap:
  disable: true
cascade:
  _build:
    render: always #never
    list: always
---
